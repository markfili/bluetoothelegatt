<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".CharacteristicActivity">

    <include layout="@layout/details_layout" android:id="@+id/layout_characteristic"/>

    <EditText
        android:id="@+id/edit_text_readable_data"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/layout_characteristic"
        android:hint="Readable data"/>

    <EditText
        android:id="@+id/edit_text_writable_data"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/edit_text_readable_data"
        android:hint="Writable data"/>

    <Button
        android:id="@+id/button_format_change"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/edit_text_writable_data"
        android:text="HEX/ASCII"/>

</RelativeLayout>
